import styled from "styled-components";

export const HeaderWrapper = styled.header`
  display: flex;
  padding-bottom: 0.5rem;

  h1 {
    margin-right: auto;
  }

  input {
    margin-right: 1rem;
  }
`