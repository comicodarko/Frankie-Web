import styled from "styled-components";

export const HeaderWrapper = styled.header`
  display: flex;
  padding-bottom: 0.5rem;

  h1 {
    margin-right: auto;
  }

  select {
    margin: 0 0.75rem;
  }
`